<%= form_for(@earning, html: {class: 'form-horizontal'}) do |f| %>
  <% if @earning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@earning.errors.count, "error") %> prohibited this todo from being saved:</h2>

      <ul>
      <% @earning.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :business, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :business, class: 'form-control' %>
    </div>
  </div>
  <div class="field form-group">
    <%= f.label :amount, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :amount, class: 'form-control' %>
    </div>
  </div>
  <div class="field form-group">
    <%= f.label :date, class: 'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.datetime_select :date, class: 'form-control' %>
    </div>
  </div>
  <div class="actions form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit %>
    </div>
  </div>
<% end %>
